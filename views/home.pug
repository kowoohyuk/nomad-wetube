extends layouts/main
include mixin/videoBlock

block content
  .home-videos
    each item in videos
      +videoBlock({
        id: item.id,
        title: item.title,
        view: item.views,
        videoFile: item.fileUrl
      })
      +videoBlock({
        id: item.id,
        title: item.title,
        view: item.views,
        videoFile: item.fileUrl
      })
      +videoBlock({
        id: item.id,
        title: item.title,
        view: item.views,
        videoFile: item.fileUrl
      })
      
